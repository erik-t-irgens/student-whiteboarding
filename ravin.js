
// Product of Array Except Self
// Problem: Given an array nums of n integers where n > 1, return an array output such that output[i] is equal to the product of all the elements of nums except nums[i].
// Example:
// Input: [1, 2, 3, 4]
// Output: [24, 12, 8, 6] // [2*3*4, 1*3*4, 1*2*4, 1*2*3]

//Check that there is a number in the array
//map over arry and mult by each element in the index
//output or result

function multByIndex(array) {
    //if array is array or array = 0
    //retrun that is array
}

const result = array.map((element, i) => elemnt * index + 1)
return result;